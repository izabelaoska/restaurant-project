import React from "react"

export const Navbar = () => {
  return (
    <nav>
      <div className="menu-buttons">
        <div>
          <button>Menus</button>
        </div>
        <div>
          <button>Login</button>
        </div>
        <div>
          <button>Find a table</button>
        </div>
      </div>
    </nav>
  )
}
